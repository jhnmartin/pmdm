# PMDM iOS

SwiftUI app. Requires Xcode 16+, targets iOS 18.0+.

```sh
open PMDM.xcodeproj
```

Select the **PMDM** scheme and a simulator, then ⌘R. ⌘U runs the Swift Testing suite.

`PMDM/` and `PMDMTests/` are synchronized folders: files added on disk show up in Xcode
automatically.
