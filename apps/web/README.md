# @pmdm/web

Next.js app for PMDM.

```sh
cp .env.example .env.local   # then set NEXT_PUBLIC_CONVEX_URL
pnpm dev                     # from this folder, or `pnpm dev:web` from the repo root
```

Opens on http://localhost:3000.
